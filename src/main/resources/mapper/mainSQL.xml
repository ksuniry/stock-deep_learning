<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.mvc.MainDao">
	<select id="commpanyList" resultType="java.util.HashMap">
			SELECT	*
			FROM	TBL_WIN_NUM
			ORDER BY CHAPTER
	</select>
	
</mapper> 